[project]
name = "aegis"
version = "0.1.0"
description = "Lightweight anomaly-based intrusion detection system using statistical baselines + ML."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]